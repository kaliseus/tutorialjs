/**
 * operadores de Java Script
 *
 * > maior que
 * >=maior que
 * < menor
 * <= menor iigual
 *
 * == igualdade (valor)
 * === igualdade strita (valor e tipo)
 * != diferente (valor)
 * !== diferente estrito (valor e tipo)
 */

const num1 = 10;
const num2 = '10';
const comp = (num1 !== num2);
console.log(comp);